[pytest]
testpaths = tests
addopts = -ra -q --cov=src/claude_artifact_manager --cov-report=html --cov-report=term
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Example: Fail if coverage drops below 70% (can be enabled later)
# cov-fail-under = 70
